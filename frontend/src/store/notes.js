import { csrfFetch } from "./csrf";

const noteReducer = (state = { notes: null }, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default noteReducer;